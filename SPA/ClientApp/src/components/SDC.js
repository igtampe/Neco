import React from "react";

export default function SDCComponent() {
    return (
        <React.Fragment>
            (List of Corporations that are not approved and are ready for approval)
            (Feed of recently approved income items (?))
        </React.Fragment>
    );

}
