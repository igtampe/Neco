import React from "react";

export default function IncomeManagementComponent() {
    return (
        <React.Fragment>

            (Select an account from a dropdown (maybe with images and some account summary stuff))
            (Tabs for each income item type, maybe a summary page or algo)

        </React.Fragment>
    );

}
